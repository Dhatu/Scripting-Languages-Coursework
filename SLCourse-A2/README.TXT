Sudhin Domala
110475495
Windows 10 lab machines

This is the answers text about the remaining word questions of the Assignment pdf.

1A)

it prints out a instance of a 8 random (rand 26) characters traveral (map) 
of any letter ranging from a to z ('a'..'z')

1B)

grep is a method of parsing a structure/variable to a specified condition in brackets
splits every whitespace within the string to be substring of every segment before/after the split calls 

2) The deep recursive and tail-recursive Perl subroutines will run into trouble with large inputs due to the similar cap of 1000 
r calls explained in A1 for the editor in place.
The non-recursive Perl subroutine will not have trouble with large inputs due to its defined parameters and not pertaining to 
recursive calls.


